import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DetailSuararakyatPage } from './detail-suararakyat.page';

describe('DetailSuararakyatPage', () => {
  let component: DetailSuararakyatPage;
  let fixture: ComponentFixture<DetailSuararakyatPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DetailSuararakyatPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
